@model Kartverket.Models.LogInData

@{
    ViewData["Title"] = "Logg Inn";
}

<div class="page-container registration-form">
    <h1>Logg Inn</h1>

    <form asp-action="LogInForm" method="post">
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger">
                <strong>Error!</strong>
                <div asp-validation-summary="All" class="text-danger"></div>
            </div>
        }

        <div class="form-group">
            <label asp-for="Brukernavn"></label>
            <input asp-for="Brukernavn" class="form-control" />
            <span asp-validation-for="Brukernavn" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Passord"></label>
            <input asp-for="Passord" class="form-control" type="password" />
            <span asp-validation-for="Passord" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary">Logg Inn</button>
        <div class="form-group" style="margin-top: 1rem;">
            <a href="#">Glemt passord?</a>
        </div>
        <div class="form-group" style="margin-bottom: 1rem;">
            <a href="RegistrationForm">Registrer deg!</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}